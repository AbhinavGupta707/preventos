# PreventOS — execution ledger

Shared truth across all sessions. Claim a WP before working it; record evidence when done.
WP definitions and acceptance criteria live in `IMPLEMENTATION_PLAN.md` §5–6.

## Status board

| WP | Title | Status | Session/branch | Evidence / notes |
|----|-------|--------|----------------|------------------|
| 1.1a | Root monorepo & CI scaffold | **done** | main (spine session) | `pnpm verify` green; import-boundary lint proven to block violations; license checker active |
| 1.1b | App shells (Expo, web, console) | **done** | main (spine session) | Expo SDK 56 app: Metro-compiled iOS bundle verified (app code present); web + console: `next build` green and served over HTTP with content confirmed; eas.json profiles in place; `pnpm verify` green across 5 workspaces |
| 1.2 | Domain model & persistence | **done** | main (spine session) | Full entity set + FHIR mapping doc; Postgres schema with identity/core separation; append-only triggers proven at DB level (UPDATE/DELETE rejected on consent/event/decision/sleep_window); migrations idempotent (re-run applies zero); one-active-enrolment-per-vertical enforced; 8/8 integration + 17/17 domain tests; unblocks 1.3, 1.4, 1.5, 4.1 |
| V.1c | QuitKit content pack — content drafting | **done** | merged (PR #5) | 149 atoms / 15 modules in `content/smoking/`; all DRAFT-stamped; schema+slot+BCT checks green incl. negative test; no instrument text (invariant 2); `pnpm verify` green; **pending canonical-schema migration (WP4.2m)** |
| V.2c | Exhale content pack — content drafting | **done** | merged (PR #2) | 8 YAML files / 45 atoms in `content/vaping/`; validated: parse + DRAFT stamps + banned-lexicon (word-boundary) + atom-id cross-refs + ladder↔product coverage + TPD ≤20mg/ml. Adult-only (E18). Product seeds are illustrative archetypes — real catalogue sourcing is an owner decision |
| V.3c | Steady content pack — content drafting | **done** | merged (PR #1) | 91 DRAFT atoms, 7 YAML files + manifest in `content/alcohol/`: diary, tomorrow-morning, social-survival, UK units, normative feedback, AVE lapse debriefs, dependence hard-stop referral scripts (inv. 4). Negative tests pass: no reduction language in hard-stop atoms; no verbatim AUDIT text; all moderation atoms contraindicated `dependence-flagged`. Assumptions: atom shape self-defined pending WP4.1 schema; pregnancy-pathway copy parked for clinical input; service numbers flagged for G3 verification |
| V.4c | Nightshift content pack — content drafting | **done** | merged (PR #3) | 7 modules, 54 atoms in `content/sleep/`, all DRAFT-stamped, YAML-validated, cross-refs resolve; claims-blocklist grep (README self-check) zero hits; `pnpm verify` green. Safety-screen questions bespoke (not instrument-derived); window-floor values deferred to engine half of WPV.4 |
| 7.1c | Safety risk corpus (~800 labelled cases) | **done** | merged (PR #6) | 843 cases, 6 categories; schema/dedup/distribution enforced by `@preventos/red-team` tests in `pnpm verify` (corrupt line proven to fail); adversarial label audit: 2 mislabels found+fixed; tier scheme {0,1,2} assumption in corpus README |
| 10.1 | Clinical reviewer brief & engagement pack | **done** | merged (PR #4) | DRAFT brief + engagement letter in `compliance/clinical-review/`; two-reviewer recommendation flagged for owner |
| 10.4 | Instrument licensing audit (SCI vs ISI etc.) | **done** | merged (PR #4) | Audit in `compliance/instruments/`; **finding: SCI paper is CC BY-NC — commercial permission needed (owner action §3.1)**; AUDIT/HSI/PHQ-2/GAD-2 cleared with conditions; ISI rejected (Mapi fees) |
| 10.6 | Privacy pack drafts (DPIAs, policy, ToS) | **done** | merged (PR #4) | 3 DPIA scaffolds + Art. 9 analysis + privacy policy + ToS drafts in `compliance/privacy/`; all DECISION/TBD items role-tagged |
| 10.10 | Claims register v0 | **done** | merged (PR #4) | `compliance/claims/` — machine-readable register (3 blocklists, 16 patterns) + checker; 25 test vectors green; forbidden sleep claim proven to exit 1 |
| 10.6/10.7b | Consumer-beta privacy + store pack | **claimed** | codex/privacy-store-pack | Adult QuitKit + Exhale beta posture; privacy decisions, retention/deletion schedule, processor register, store review pack, Apple/Google health-app checklist, required disclaimers; explicitly no formal clinical sign-off claim; Steady/Nightshift/wearables/youth/live escalation blocked pending stronger review; `pnpm verify` green; PR blocked locally by invalid `gh` token |

| 4.1 | Content-atom schema & store | **done** | main (spine session) | `@preventos/content`: zod schema (meta+defaults+atoms, bcttv1 tags, COM-B, slots); draft content unservable in production (no override by design); deterministic catalog hash for cohort pinning; ratchet gate over `content/` in CI (LEGACY_PACKS must shrink to empty); 14/14 tests |
| 1.4 | Event backbone & outbox | **done** | main (spine session) | `@preventos/events`: 16-type strict catalogue (id-refs only — erasure invariant), transactional publish (event+outbox atomic), SKIP LOCKED dispatcher with backoff→failed; 5/5 integration tests |
| 1.3 | Consent service & data rights | **done** | main (spine session) | `@preventos/consent`: deny-by-default checks, grant/revoke evented, GDPR export bundle, erasure with audit carve-outs (identity deleted, person anonymised, mutable rows deleted, append-only audit retained); 5/5 integration tests |
| 5.1 | Rules engine v0 | **done** | main (spine session) | `@preventos/decisions`: zod-validated rule sets, deterministic evaluation (byte-identical replay), content-addressed policy hash, shadow-diff mode |
| 5.2 | Burden governor + arbitration + scheduler | **done** | main (spine session) | One cross-vertical budget (≤3/day, 60-min gap, quiet 21:30–08:00 — pending WP10.3 sign-off); starvation-guarded arbitration (fairness proven over 25 rounds); deterministic decision-point generator (6-week time-warp test) |
| 5.3 | Relapse state machine | **done** | main (spine session) | Universal machine, per-vertical lapse definitions in config; lapse never skips debrief; days-won monotone non-decreasing (property-tested) |
| 1.5 | Auth port, RBAC, k-anonymity | **done (port)** | main (spine session) | `@preventos/auth`: provider port + fake provider; deny-by-default matrix (analyst provably excluded from every row-level action); k≥5 suppression. **Clerk adapter awaits owner-created API keys** |
| 4.2m | Content pipeline + canonical migration (WP4.2 + WP4.4) | **done** | wp/4.2m-content-pipeline | Validator CLI (`pnpm content:validate`) in `pnpm verify`, per-pack; claims lint consumes WP10.10 register (live injection of "treats insomnia" proven to exit 1); sign-off schema enforced; FK reading-age scorer; schema vocab +7 types, strict; all 4 packs migrated (149+45+91+54 atoms), LEGACY_PACKS ratchet EMPTY, one merged catalog; `@preventos/instruments`: HSI/AUDIT-C/TTFV score published vectors, hash-locked verbatim items, SCI blocked-pending-license + proven unreachable. Assumptions: vaping/alcohol BCT+COM-B DRAFT; PHQ-2/GAD-2 entries deferred |
| WS7 | Safety subsystem (7.1, 7.2, crisis-static, 7.4) (wave 2) | **done** | wp/ws7-safety-subsystem | `@preventos/safety`: deterministic de-obfuscating classifier (no bypass param — inv. 1) → **tier-1 recall 1.00 over all 843 cases** (gate ≥0.95), tier-0 FP 0.0%, p95 <2ms. WP7.2 scripted crisis flows over riskClass×tier×vertical, alcohol→E17 (inv. 4); `apps/crisis-static` isolated (zero platform imports, eslint exit-1 proven; HTTP 200, no JS, 2am-safe). WP7.3 escalation queue over `core.escalation_case` (SLA clocks, guarded transitions, closed=immutable, events) — open→claim→close demo vs real Postgres. WP7.4 red-team harness (corpus + 24 jailbreaks) → required CI check; ratchet proven red on unrecorded miss. Resource numbers pending G3 sign-off |
| WS2 | Mobile app (2.1–2.6) | **done** | wp/ws2-mobile-app | apps/mobile (expo-router, SDK 56). Proven on a real iOS Release build (EAS `preview-simulator` + local Release, both NO Metro): Maestro `golden-path` 40 steps — ≤90s smoking intake→BFO, HSI verbatim (inv. 2), push primer→defer, today single-NBA card, day-1 milestone once, rescue ≤1 tap from today, if-then plan, cross-programme savings £9.75 on device, days-won via `decisions.daysWon`. Maestro `crisis-gate` — tier-1 text routes to scripted crisis screen, coach provably never replies (inv. 1), normal msg still streams. Offline: rescue+crisis import zero network. 31 mobile + instrument tests. Assumptions: StyleSheet not NativeWind; Steady gated off pending AUDIT+hard-stop (inv. 4); sleep intake bespoke Qs; EAS device build needs owner Apple creds; **instruments adapted to canonical 4.2m package at integration (see merge commit)** |
| SVC | Services assembly (apps/api + apps/worker) (wave 2) | **done** | wp/svc-services | apps/api (Fastify): enrolment, consent grant/revoke/check, craving/drink/sleep-diary logs, plan CRUD — all via spine packages, auth-port middleware (FakeAuthProvider), zod at every boundary, rate limiting; 19 integration tests incl. 401/403/409/429 negatives. apps/worker: outbox dispatch loop + decision tick (decisionPoints→evaluateRules→arbitrate→consent+burden gates→contact+DecisionRecord), idempotent; 8 integration tests. Demo A proven over real HTTP against Postgres. Assumptions: craving log = inbound contact_record; plan deletion un-evented; default anchor rule set lives in apps/worker until vertical packs ship rules; prod default tz Europe/London; proactive contacts `queued` (push = WP2.5) |
| WS8 | Outcomes, analytics & evidence (8.1, 8.3 skeleton, 8.4) | **done** | wp/8-outcomes-evidence | `@preventos/outcomes`: versioned content-hash-pinned definitions (Russell-Standard 4w ITT, vaping pp7/pp30, AUDIT-C delta + drinking days, sleep SE/SOL/WASO; SCI deferred pending WP10.4 license) evaluated against 50-journey fixture cohort (43/43 tests). Experiments: deterministic sha256 assignment, mandatory escalation guardrail, safety surfaces unrepresentable by construction. Console `/evidence`: k≥5 suppression server-side, proven at runtime (zero n=2 groups in served HTML). **WP8.2 analytics instrumentation still open** — needs live events from apps |
| 3.1 | Marketing site (web) | **done** | wp/3.1-web-surface | 4 landing pages + savings & sleep-debt calculators + 4-article SEO hub + waitlist; claims-register lint wired into `lint` AND `prebuild` (forbidden claim proven to fail `next build`); conversion events first-party (NDJSON sink pending SVC); Lighthouse 99–100/100/100/100 on marketing pages; unit maths pinned to NHS published examples |
| 3.2 | Web app parity (started) | **partial** | wp/3.1-web-surface | Onboarding (Steady dependence signpost gate proven unbypassable), today surface, sleep diary (4-tap, verified in-browser), drink log (UK units), consent centre (deny-by-default, export, 2-step erase — verified in real browser); axe-core zero violations on all 16 pages; local-first store pending SVC sync; remaining flows outstanding |
| AV.2 | Companion state engine (avatar) | **done** | main (integration session) | `@preventos/companion`: pure `companionView(snapshot)` → mood/dialogueSlot/evolutionStage/animation. Ethical rules enforced+tested (10/10): steps aside in crisis (invisible+silent), lapse→care never blame, deterministic days-won evolution (no loot), returns governed slot ids not copy. `demo/index.html`: runnable animated SVG reference (7 moods, days-won evolution, 4s/6s breathing, reduced-motion). Rendered + verified inline |
| AV.3 | Companion into mobile (home/SOS/coach) | **done** | main (consolidation) | `@preventos/companion` wired into mobile via a pure mapper (`src/core/companion.ts`) + a code-built animated RN placeholder (`src/ui/Companion.tsx`, Animated/AccessibilityInfo only — no Rive yet). Present on **home** (mood from days-won/lapse/milestone/time), **SOS rescue** (co-breathing, night-aware tint), **coach**. Engine stays authoritative (no duplicated mood/evolution logic); placeholder is **silent** (renders no ungoverned copy — inv. 3). **Invariant 1 re-proven at app layer:** crisis context → `visible:false`/silent (unit test), crisis screen mounts no companion, Maestro `crisis-gate` asserts companion present on coach + `assertNotVisible` on the crisis surface; golden-path asserts companion on home. 8 companion unit tests; `pnpm verify` green; **iOS Metro bundle re-exports clean** |
| AV.1 | Companion Rive rig | open | — | commission the Rive character rig (owner/design); the AV.3 placeholder swaps out for it behind the same `companionView` contract |
| WS6 | AI coach policy proxy (WP6.1 + WP6.3 + W3-SAFEPORT) | **in_review** | wp/ws6-coach-policy-proxy | `@preventos/coach`: deterministic pre-filter (843-classifier) → LLM (provider port; Claude `claude-opus-4-8` adapter via official `@anthropic-ai/sdk` + deterministic Fake) → post-filter (output re-classified + governed claims register `coach-fences` scope). System prompt = spotlighting + instruction hierarchy (OWASP LLM01:2025; cited in README). 100% logging → `core.coach_interaction` (migration 0003; erasable free text wired into GDPR export+erasure). **Crisis tier 1 AND 2 BYPASS the LLM** — proven live: `provider.generate` calls +0 across the crisis turn; scripted flow + escalation case opened. Fail-closed: provider error→fallback, blocked output→safe substitute, log failure→turn fails. `apps/api` `POST /coach/messages` (auth+consent gated). Proven over **live HTTP vs Postgres** + 28 api & 16 coach tests; post-filter block-recall ≥0.98. Assumptions: `COACH_MODEL` env-overridable (defaults opus-4-8 per SDK guidance); Claude adapter dormant w/o `ANTHROPIC_API_KEY` (CI → Fake, no spend); WP6.2 full MI-eval ≥90% + WP6.4 proactive deferred (next). **Note: branch based on `3a8b19d` which the parallel W3-SECHARD session's `git add -A` swept my safety→safety-core moves into — see session log** |

### Wave 3 — from deep review (see REVIEW_FINDINGS.md for full register)
| WP | Title | Status | Notes |
|----|-------|--------|-------|
| W3-WIRE | Connect apps to live API | **done** | wp/w3-wire-live-api | New `@preventos/api-client` (shared Result-typed fetch client, RN/browser/node) is the single HTTP contract. apps/api gains gated `POST /dev/session` (Clerk stand-in, 404 in prod). **mobile**: `FetchApi` adapter (selected by `EXPO_PUBLIC_API_URL`) — smoking intake stands the journey up server-side (consents+enrolment+quit plan); MockApi stays offline/test default; rescue stays fully offline. **web**: app flows mirror writes via same-origin `/api/app` proxy → apps/api (localStorage = offline cache); `PREVENTOS_API_URL` toggles. **worker**: `makeAuditHandlers` registered in main.ts → events consumed (not write-only). **console**: `/evidence` renders live counts from real event/decision/contact/enrolment rows. Proven: `pnpm verify` 21/21 green (+19 new tests across api-client/api/worker/console/mobile/web); journey-sim Demo A PASS over real HTTP; console `/evidence` curl rendered real journey data; web `/api/app` curl → DB rows (alcohol enrolment + drink + drink.logged event). **Scope boundary**: marketing `.data/*.ndjson` (waitlist + web funnel events) left as-is — no domain endpoint exists; routing them is WP8.2 analytics + a waitlist-store owner decision. Session-token persistence is in-memory (dev person per launch) pending Clerk (WP1.5) |
| W3-GUARDS | Worker fail-fast on dangling refs | **done** | merged (#15): `assertRuleSetResolvable` validates rule-set refs (atom/sequence/outcome/vertical) against the content catalog + outcome registry at worker boot AND in CI; negative boot proven (empty content → exit 1). 9 placeholder refs re-pointed to real atoms; outcome_ref namespace reconciled + `OUTCOME_REF_IDS` registry; `content:validate` rejects dangling outcome_ref. Parked: smoking 7d/12w/6m clinical params = declared ids pending WP10.3 |
| W3-STEADY | Alcohol hard-stop in rules engine (inv4) | **done** | merged (#14): unbypassable priority-100 hardstop preempts arbitration+burden+proactive-consent → referral; `isContraindicated` gate at contact-send blocks every moderation atom for `dependence-flagged`; RuleContext carries `auditScore`+`flags` from `enrolment.assessment` jsonb (migration 0003, written on AUDIT-C intake, flags ≥11). Runtime-proven vs Postgres incl. defense-in-depth (gate blocks even with the rule removed); worker fail-closed if catalog missing. Assumptions in session log. **gates Steady go-live** |
| W3-SAFEPORT | Extract pure classifier for mobile | **done** | merged via WS6 (#17): `@preventos/safety-core` (db-free, lint-enforced no-db boundary) is the one 843-validated classifier; mobile `classifyOutbound`→it, api/coach use it. Closed 3 ideation-phrasing gaps with guarded tier-2 rules; corpus recall 1.00 / FP 0.0%. **PR #16 (subpath variant) superseded by this package design — closed unmerged** |
| WS6 | Guardrailed AI coach behind policy proxy | **done** | merged (#17): `@preventos/coach` pre-filter(classifier)→LLM(Claude port + Fake)→post-filter(claims fences); crisis bypass proven (LLM never called); 100% logging to `core.coach_interaction` (erasable); `POST /coach/messages`; 16 coach + 7 api-coach tests |
| W3-SECHARD | Bounded security hardening | **done** | merged (#13), follow-up on main: waitlist trusted-XFF rate-limit key plus bounded in-memory bucket map (TTL prune + max buckets); events catalogue enum/regex hardening (erasure invariant); pnpm.overrides esbuild/postcss/uuid → `pnpm audit` 4→0 |
| W3-NODECRYPTO | RN-safe hashing (unblocks iOS bundle) | **done** | merged: `@preventos/shared` `sha256Hex` (pure JS, @noble/hashes MIT), byte-identical to node:crypto (pinned test + all hash-assertion suites); 5 call sites swapped. **`expo export --platform ios` succeeds** (2.9MB Hermes bundle, zero node:crypto errors). The mobile app now bundles for iOS |
| W3-DATA | Data-integrity bounded | **done (partial)** | merged: `0005` enrolment(person_id) index; renumbered dup-0003 migration; **diary-mutability DECISION**: contact/drink/sleep-diary stay mutable (DLR + retro-fill), audit lives in append-only event stream — no triggers (review fix rejected). Deferred: plan version/audit trail (lower priority) |
| W3-TESTCI | CI/test hardening | **done (partial)** | merged: `@preventos/db` `resetTestDatabase` advisory-locks CREATE/DROP across all 11 integration suites — 23/23 uncached parallel, no race. Deferred: app-layer verbatim + hard-stop integration tests |
| WP8.2 | Analytics + waitlist store | **done** | main (consolidation) | New isolated **`marketing` Postgres schema** (migration `0006`): `waitlist_signup` + `funnel_event`, **no FK to core.person** (leads ≠ persons — proven structurally). `@preventos/db` repos + per-programme/per-event count helpers. `apps/api` public **`POST /marketing/waitlist`** + **`/marketing/events`** gated by the authoritative `.strict()` allow-list (programme/event enums; funnel props = short scalars only, ≤10 keys — structurally no free text/identifiers/Art.9). `apps/api-client` `joinWaitlist`/`recordFunnelEvent`. **Web `.data/*.ndjson` sinks now route to the domain store** when `PREVENTOS_API_URL` set (NDJSON stays the offline/dev fallback). Console `/evidence` renders **per-programme waitlist + conversion-event counts** (coded aggregates, no email/per-lead). **Payload privacy audit** (`compliance/privacy/marketing-funnel-privacy-audit.md`): no special-category to 3rd parties (first-party only, no processors). +6 api, +marketing db, +2 console funnel tests; both Next apps build green; `pnpm verify` 23/23. **Owner decision: analytics provider (none wired — first-party only by design); DPO retention/lawful-basis review flagged.** |
| AV.3 | Companion into mobile screens | **done** | main (consolidation) | `companionView` is wired into mobile home/SOS/coach with an animated RN component (code-built placeholder until the Rive rig); crisis step-aside re-proven. Duplicate wave-table row corrected in the post-consolidation cleanup |
| WP6.2 | Coach MI-adherence eval (≥90%) | **done** | main (consolidation) | `@preventos/coach` `runMiEval` runs the coach over `MI_CORPUS` (28 tier-0 turns × smoking/vaping/alcohol/sleep) through the full pre→LLM→post pipeline; deterministic MI rubric (`scoreMiAdherence`: brief / ≤1 open question / reflect-affirm / no righting-reflex / no shame). CI gate vs **Fake provider** (no key, zero spend): overall **92.9% ≥ 90%**. Rubric calibrated both ends — 2 in-corpus sub-par replies fail (gate is a real floor) and `NON_ADHERENT_CONTROLS` score 0% (rubric has teeth). Same harness scores a real provider when a key lands. 6 eval tests; corpus guarded all-tier-0 |
| FIXED | API safety gate + drizzle SQLi | **done** | main (integration session) | screenInboundText classifies inbound free text, tier-1→escalation case (2 tests); drizzle 0.44.2→0.45.2 |
| W3-FIREWORKS | Coach Fireworks provider + selection chain | **done** | main (consolidation) | `packages/coach/src/providers/fireworks.ts`: OpenAI-compatible (base `https://api.fireworks.ai/inference/v1`), reads `FIREWORKS_API_KEY`, default `COACH_MODEL=accounts/fireworks/models/llama-v3p3-70b-instruct`; per-request `LlmRequest.model` override (frames can route simple↔complex turns; Claude adapter honours it too). `apps/api` coach-deps selects **fireworks ?? claude ?? Fake** — no key → Fake (zero CI spend). 8 unit tests w/ injected fetch; `pnpm verify` green. **Owner action: provide `FIREWORKS_API_KEY` to activate.** |
| WPV.4e | Nightshift titration engine/API slice | **partial** | codex/nightshift-titration-engine | Pure titration engine (`@preventos/domain`) computes append-only sleep-window recommendations from recent diary entries; DB repos expose latest/recent/append helpers; `apps/api` adds consent-gated `POST /sleep/windows`; `@preventos/api-client` exposes `createSleepWindow`. Safety-sensitive occupation and excessive daytime sleepiness apply conservative floor/hold + signpost flags. `pnpm verify` green. UI surfacing continues in WPV.4f; remaining clinical scope: sign-off for constants, shift-worker protocol/wearable corroboration. |
| WPV.4f | Nightshift sleep-window UI surfacing | **done (code, gated)** | merged from codex/steady-nightshift-buildout | Web sleep diary can request and display the latest sleep-window recommendation via the same-origin sync proxy; local store persists the returned window; safety-sensitive/sleepiness flags are exposed in the request form. Mobile now has a native four-field sleep diary + API-backed sleep-window result surface using `logSleepDiary`/`createSleepWindow`, with local persistence, safety-sensitive/sleepiness toggles, and wellbeing-only copy. Web proxy now returns `sleepWindow` payloads from `/api/app`. Pending: clinician sign-off for constants, shift-worker protocol/wearable corroboration, and public enablement decision. |
| G1-QA | Safety QA launch-gate test gaps | **done** | merged from codex/safety-qa-launch-gates | Added app/API edge tests for elevated crisis routing, mobile HSI verbatim-render contract, web Steady referral-only copy assertions; re-proved worker alcohol hard-stop integration. `pnpm verify` green. |
| UI-QE | QuitKit + Exhale beta UI polish | **done** | merged from codex/quitkit-exhale-beta-ui | Mobile-first beta wedge polish: warm companion tokens/primitives, Today/NBA/days-won/savings surfaces, public onboarding scoped to QuitKit+Exhale, accessible SOS/coach controls, progress empty state, privacy tab, Steady/Nightshift marked private, crisis UI unchanged/direct. Verification: mobile typecheck/lint/test green; `pnpm verify` green after sandbox-escalated rerun for tsx IPC. |
| UI-VQA | Mobile visual QA polish | **done** | merged from codex/mobile-visual-qa-polish | iPhone 17 Pro simulator/development-build pass after QuitKit+Exhale UI merge. Captured onboarding, programme picker, Today, SOS, Coach, Progress, Privacy, and crisis surfaces under `artifacts/mobile-visual-qa/`; fixed FAB overlap on onboarding/privacy/Today and Dynamic Island collisions on SOS/crisis. Verification: mobile typecheck/lint/test green; `pnpm verify` green. |

Statuses: open → claimed(session/branch) → in_review(PR) → done(evidence).

## Session log
- 2026-06-18 · MOBILE VISUAL QA POLISH (codex/mobile-visual-qa-polish): built and installed the current Expo development build on iPhone 17 Pro simulator, captured required beta UI surfaces, and fixed visual issues only: route-aware SOS/FAB hiding on onboarding/privacy, compact circular SOS control on tabs, and extra top spacing for SOS/crisis on Dynamic Island devices. Steady/Nightshift remain private-visible only; crisis copy and companion absence preserved. Verification: `pnpm --filter @preventos/mobile typecheck`, `lint`, `test` (49 tests), and full `pnpm verify` green. Screenshots: `artifacts/mobile-visual-qa/*-fixed.png`.
- 2026-06-18 · STEADY + NIGHTSHIFT BUILD-OUT (codex/steady-nightshift-buildout): pushed early branch claim from an isolated worktree. Nightshift mobile now has a native morning sleep diary (`/sleep-diary`) with local-first diary persistence, API-backed sleep-window requests, result display, and safety-sensitive/daytime-sleepiness flags; Today routes sleep users to the diary instead of the generic check-in. Steady mobile now routes the programme picker to a referral-only hard-stop surface, with no enrolment/log/coach unlock. Web now gates Steady and Nightshift behind explicit internal flags (`NEXT_PUBLIC_PREVENTOS_ENABLE_STEADY_INTERNAL`, `NEXT_PUBLIC_PREVENTOS_ENABLE_NIGHTSHIFT_INTERNAL`), hides stale gated local state, blocks direct public drink/sleep tooling with boundary copy, and threads optional drink-log context through the API safety-screened field. Fixed `/api/app` to return sleep-window payloads to the web UI. Verification: mobile/web typecheck+lint+tests green, claims register self-check green, web build green, `pnpm content:validate` green (existing reading-age warnings only), full `pnpm verify` green. Remaining blocked: Steady public moderation and Nightshift public sleep-window features stay gated pending owner/clinical acceptance; no sleep treatment claims added; no alcohol hard-stop threshold changed.
- 2026-06-18 · MASTER LAUNCH READINESS COORDINATION (`codex/launch-readiness-coordination`): added `LAUNCH_READINESS.md` as the master board for the consumer beta push. It defines the recommended QuitKit+Exhale wedge, G1-G5 status, clinical/legal blockers under a no-formal-signoff posture, parallel-safe agent workstreams, conflict rules, merge order, and the Design/UI Recon prompt. Repo state at start: `main` synced with `origin/main`; only local untracked item is `Design Inspiration/`; remote feature branch still present: `origin/codex/nightshift-ui-surfacing`.
- 2026-06-18 · SAFETY QA LAUNCH GATES (codex/safety-qa-launch-gates): closed deferred G1 test gaps with tests only. Mobile chat now proves alcohol withdrawal/dependence-style elevated risk never becomes a coach request; mobile HSI onboarding is locked to `@preventos/instruments` text rather than local copy; API drink-log context now proves tier-2 withdrawal risk opens an escalation case; web Steady entry/drink-log copy is pinned to referral-only language. Re-ran worker hard-stop integration. Verification: mobile/web/api targeted tests + worker tests + touched lint/typecheck + full `pnpm verify` green.
- 2026-06-18 · PRIVACY/STORE COMPLIANCE (codex/privacy-store-pack): prepared consumer-beta disclosure and store-review pack for adult QuitKit + Exhale without claiming formal clinical sign-off. Added beta privacy decisions, retention/deletion schedule, processor register, store review pack, Apple/Google checklist, and required no-medical-device/no-diagnosis/no-emergency/consult-professional disclaimers. Updated privacy policy/ToS/DPIAs/claims register to align; high-risk Steady/Nightshift/wearables/youth/live escalation remain blocked pending owner/legal/clinical review. Verification: claims register self-check, `pnpm content:validate`, and full `pnpm verify` green (existing content reading-age warnings only). Branch pushed; PR creation blocked locally because `gh auth status` reports an invalid token.
- 2026-06-18 · RELEASE/INFRA BETA (codex/release-infra-beta): claimed release docs/config lane in an isolated worktree; added root `.env.example`; added `docs/beta-release-runbook.md` covering GitHub Actions `ci / verify`, local vs CI verification, Clerk activation path, Fireworks/LLM activation path, EAS/TestFlight/Play internal testing, and API/web/console/worker deployment checklist. Verification: `pnpm check-licenses`, `pnpm content:validate` (existing reading-age warnings only), full `pnpm verify` green.
- 2026-06-18 · QUITKIT + EXHALE UI POLISH (codex/quitkit-exhale-beta-ui): pushed early branch claim, then polished `apps/mobile` beta wedge only. Added terracotta/sage/peach tokens and shared primitives (`ActionCard`, `ProgrammeChip`, `ProgressMetric`, `SegmentedControl`, `SafetyInterruption`); refreshed onboarding, Today, SOS, coach, progress, persistent Rescue, and a new Privacy tab. Public setup now offers QuitKit and Exhale only; Steady/Nightshift are visibly private/gated; validated HSI text and crisis surfaces were not changed. Verification: `pnpm --filter @preventos/mobile typecheck`, `lint`, `test` green (45 mobile tests); full `pnpm verify` green after rerunning outside the sandbox because `tsx` IPC pipe creation was blocked in the sandbox. Visual QA: code-level responsive/accessibility pass only; no simulator screenshot captured in this session.
- 2026-06-18 · WPV.4 NIGHTSHIFT UI SURFACING (codex/nightshift-ui-surfacing): web sleep diary now has a sleep-window section that unlocks after 5 diary entries, calls `/api/app` → `apps/api` `POST /sleep/windows`, stores the returned window locally, and displays duration + safety signpost flags. The web sync schema/proxy carries a typed `sleepWindow` action. Mobile API port/live adapter/mock adapter now expose `logSleepDiary` and `createSleepWindow`, with adapter tests, so the native four-field sleep diary screen can land next without another backend contract pass. Verification: web api-sync/a11y/typecheck/lint, mobile fetch-api/typecheck/lint. Pending: native mobile form/result surface and clinical sign-off constants.
- 2026-06-18 · WPV.4 NIGHTSHIFT TITRATION ENGINE/API SLICE (codex/nightshift-titration-engine): added a pure sleep-window titration engine with 5-of-7 diary sufficiency, mean sleep + buffer initialisation, efficiency-based expand/restrict/hold, conservative safety floor, and excessive-daytime-sleepiness hold/signpost. Wired append-only persistence via `sleep_window`, added consent-gated `POST /sleep/windows`, and exposed it through `@preventos/api-client`. Verification: targeted domain/api-client/db/api tests plus full `pnpm verify`. Pending: UI surfacing in mobile/web, clinical sign-off of constants, shift-worker protocol and wearable corroboration.
- 2026-06-18 · POST-CONSOLIDATION CLEANUP (main): readiness fixups for the next build step. Bounded the web waitlist rate-limiter bucket map (TTL pruning + hard max buckets) while preserving trusted-XFF spoof resistance; hardened `scripts/check-licenses.mjs` so `pnpm verify` still enforces the no-GPL/AGPL rule when pnpm's local store metadata is unavailable by falling back to installed package manifests; tracked `AGENTS.md` (byte-identical to `CLAUDE.md`) for Codex session discovery; corrected the stale duplicate AV.3 ledger row. Verification: web rate-limit/client-IP tests, license check fallback, full `pnpm verify`.
- 2026-06-13 · CONSOLIDATION (spine session): autonomous consolidation run. (1) Fireworks coach provider — `@preventos/coach` gains an OpenAI-compatible `FireworksCoachProvider` (base fireworks `/inference/v1`, `FIREWORKS_API_KEY`, default Llama-3.3-70B, per-request model override); `apps/api` selects fireworks ?? claude ?? Fake (no key → Fake, zero spend); 8 fetch-injected unit tests; `pnpm verify` green. (2) AV.3 — `@preventos/companion` wired into mobile home/SOS/coach via a pure mapper + a code-built animated RN placeholder (silent: no ungoverned copy, inv. 3). Invariant 1 re-proven at the app layer: crisis context → invisible+silent (unit test), crisis screen mounts no companion, Maestro asserts companion absent on the crisis surface. iOS Metro bundle re-exports clean (3MB Hermes, `stepped_aside`/companion in the string table). (3) WP6.2 — `runMiEval` + deterministic MI rubric over a 28-turn per-vertical corpus, Fake-provider CI gate at 92.9% ≥ 90%, calibrated by 2 in-corpus sub-par slips + clearly-bad negative controls (0%). (4) WP8.2 — isolated `marketing` schema (migration 0006, no FK to core.person) + `apps/api` public allow-list endpoints + api-client methods; web `.data` sinks now route to the domain store (NDJSON = offline fallback); console `/evidence` shows per-programme waitlist + conversion counts; privacy audit (first-party only, no special-category to 3rd parties). All 23 suites green; both Next apps build.
- 2026-06-13 · INTEGRATION (spine session): merged all six wave-3 PRs to main in dependency order (#13 SECHARD → #17 WS6/coach+safety-core → #15 GUARDS → #14 STEADY → #18 WIRE); #16 closed as superseded by #17's safety-core package. Hand-merged the 4-way `apps/worker` overlap (refguard boot + hardstop preemption + audit handlers + atomFor) and `apps/api/server.ts` route-deps. **`pnpm verify` 23/23 green; CI green on the now-PUBLIC repo (2m14s); Demo A PASS end-to-end vs Postgres.** Repo made public to free GitHub Actions minutes. Worktrees/branches cleaned up.
- 2026-06-12 · spine session: plan v3 adopted; WP1.1a built and verified; repo initialized.
- 2026-06-13 · integration session: merged 6 wave-2 PRs (CI green), resolved @preventos/instruments collision (kept canonical, adapted mobile, fixed index-vs-score scoring bug); built @preventos/companion (AV.2) + animated demo; ran 7-dimension adversarial review (40 agents) → REVIEW_FINDINGS.md; fixed CRITICAL API safety gate + HIGH drizzle SQLi.
- 2026-06-13 · WS6 session (worktree wp/ws6-coach-policy-proxy): guardrailed coach **policy-enforcement proxy** (WP6.1+6.3) — `@preventos/coach` pre→LLM→post + 100% logging (`core.coach_interaction`, GDPR-wired) + crisis bypass; `@preventos/safety-core` (W3-SAFEPORT) extracted + classifier strengthened (3 ideation rules, 843-corpus unregressed); `apps/api` `/coach/messages`. Research-first: OWASP LLM01:2025 + Microsoft spotlighting + instruction hierarchy (cited in packages/coach/README). Proven live over HTTP vs Postgres + `pnpm verify` green. **MULTI-SESSION COLLISION:** the shared root checkout was switched off `main` onto `wp/W3-SECHARD-security-hardening` by a parallel session mid-work, and its `git add -A`/commit `3a8b19d` swallowed my 16 safety→safety-core file renames. I isolated my work into this worktree (pathspec-stash → worktree off `3a8b19d` → clean reinstall); the W3-SECHARD session's uncommitted changes were left untouched in the main checkout. Several parallel WP branches now exist on origin (w3-wire, w3-guards, w3-steady, w3-safeport[dup], w3-sechard×2) — **leads must serialise spine merges + dedup W3-SAFEPORT**.
- 2026-06-12 · V.4c session (worktree): Nightshift content pack drafted under wellbeing framing (E16); claims blocklist seeded in content/sleep/README.md pending WP4.2 lint/WP10.10 register.
- 2026-06-12 · SVC session (worktree wp/svc-services): platform services assembled (apps/api, apps/worker, tools/journey-sim); Demo A loop proven end-to-end over HTTP against Postgres.
- 2026-06-12 · 4.2m session (worktree): content pipeline + canonical migration + instruments registry; ratchet emptied; claims lint live in CI.
- 2026-06-13 · WS7 session (worktree wp/ws7-safety-subsystem): `@preventos/safety` classifier + crisis flows + escalation queue, `apps/crisis-static`, WP7.4 red-team harness/ratchet. Tier-1 recall 1.00 on 843 cases; runtime-proven. Assumption: corpus tier {0,1,2} adopted; resource phone numbers flagged for G3.
- 2026-06-12 · WS3 session (worktree wp/3.1-web-surface): WP3.1 built+verified, WP3.2 started. Assumptions: waitlist/events persist to gitignored `.data/*.ndjson` until SVC; app flows local-first (localStorage) until SVC sync; unit/diary maths in `apps/web/lib` earmarked for extraction to WPV.3/V.4; `/app` intentionally noindex.
- 2026-06-13 · W3-SECHARD session (worktree wp/W3-SECHARD-hardening): bounded security hardening — waitlist XFF spoof fix (trusted-hop rate-limit key, HTTP-proven), events catalogue coded-value hardening (6 fields enum/regex, erasure invariant), transitive dep bumps via pnpm overrides (audit 4→0). `pnpm verify` green; both Next builds green. NOTE: a concurrent W3-SAFEPORT session is staging `packages/safety-core` in the **shared root checkout**; my initial claim branch `wp/W3-SECHARD-security-hardening` accidentally captured its partial rename, so this WP shipped on a clean branch off origin/main instead — the orphan claim branch can be deleted by the owner.
- 2026-06-13 · W3-GUARDS session (worktree wp/w3-guards-refguard): worker fail-fast ref guard (`apps/worker/src/refguard.ts`) at boot + CI; DEFAULT_RULE_SET re-pointed off 9 placeholders to real atoms + canonical outcome id; `outcome_ref` namespace reconciled to outcome-id convention with a new `OUTCOME_REF_IDS` registry validated in `content:validate`. Negative boot runtime-proven (empty content → exit 1). Touches no spine package. Parked: smoking 7d/12w/6m/cigs-per-day clinical params (WP10.3) — declared as recognized ids, not invented. Note: re-pointed alcohol anchors use moderation atoms; W3-STEADY's contraindication gate (separate WP) still governs Steady go-live.
- 2026-06-13 · W3-STEADY session (worktree wp/w3-steady-alcohol-hardstop): alcohol dependence hard-stop operationalised in the rules engine (inv4). New: `unbypassable` rule flag + `mandatoryCandidate` preemption (`packages/decisions`); `deriveAlcoholFlags`/`DEPENDENCE_FLAG`/threshold in `contraindication.ts`; `enrolment.assessment` jsonb (migration 0003) written by enrolment API on AUDIT-C intake; worker threads `auditScore`+`flags` into `evaluateRules`, preempts to the referral, and runs the `@preventos/content` `isContraindicated` gate before any contact-send (worker fail-closed at boot if the catalog can't load). **Assumptions (record/flag):** (1) hard-stop threshold = AUDIT-C **≥11** (instrument "possible dependence" band; moderation stays for ≤10 tiers per E17) — **pending WP10.3 clinical sign-off**, change the constant never the gate; (2) the unbypassable referral bypasses the burden governor AND `proactive_contact` consent (delivered under the `programme_delivery` consent enrolment already requires — a scripted safety referral is core delivery, not a JITAI nudge) — flag for privacy pack (10.6)/G3; (3) hard-stop fires on each due alcohol decision point — longitudinal "referral-once → signpost-only mode" cadence is WPV.3/W3-WIRE scope; the proven safety property is *never moderation, always referral*; (4) `dependence-flagged` literal is shared by code + content YAML, guarded by a content↔engine alignment test. **Multi-session note:** built in a worktree because the shared root checkout was concurrently on `wp/W3-SECHARD` with `wp/w3-safeport` changes uncommitted; **`apps/worker` also touched by `wp/w3-wire-live-api`** — expect a small merge there (dispatcher/main handler wiring vs my tick/ruleset/catalog wiring).
- 2026-06-13 · W3-WIRE session (worktree wp/w3-wire-live-api): built `@preventos/api-client` (shared client) + gated dev-session endpoint; wired mobile (FetchApi), web (`/api/app` proxy), worker (audit handlers), console (live `/evidence`). `pnpm verify` 21/21 green. Runtime-proven: Demo A PASS over real HTTP (journey-sim); console rendered real journey rows; web proxy curl landed rows in Postgres. Assumptions recorded: dev session in-memory pending Clerk (WP1.5); marketing `.data` NDJSON sinks parked for WP8.2; web sleep diary maps get-up time to both final-wake and rise. Coexisted with a local (unpushed) wp/w3-safeport worktree — no file overlap.
